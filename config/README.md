# Configuration Directory

This directory is designated for custom application-level configuration modules.

Note: Tool-specific configuration files (such as `vite.config.js`, `vitest.config.js`, `eslint.config.js`, `vercel.json`, and `docker-compose.yml`) remain at the project root as required by their respective CLI runtimes and build tools.
